{
  "name": "kafu-system",
  "version": "1.0.0",
  "description": "KAFU System - Competency Framework Management",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["competency", "framework", "management", "react", "nodejs"],
  "author": "KAFU System",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
